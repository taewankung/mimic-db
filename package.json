{
  "name": "mimicdatabox",
  "version": "0.1.0",
  "description": "A tiny in-memory mock data database for tests",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "require": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "node -e \"['dist', 'dist-test'].forEach((dir) => require('fs').rmSync(dir, { recursive: true, force: true }))\"",
    "build": "npm run clean && tsc -p tsconfig.build.json",
    "test": "npm run clean && tsc -p tsconfig.test.json --pretty false && node --test dist-test/test",
    "prepack": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "mock",
    "database",
    "in-memory",
    "testing",
    "typescript"
  ],
  "sideEffects": false,
  "devDependencies": {
    "@types/node": "^20.17.0",
    "typescript": "^5.6.3"
  }
}